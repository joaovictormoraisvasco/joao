const form = document.getElementById("form")
const lista = document.getElementById("climas")

let clima = JSON.parse(localStorage.getItem("cadastro")) || [];

function renderInformaçoes( {
lista.innerHTML = "";
clima.for each((inter))
})
